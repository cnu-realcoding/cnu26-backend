# ========== DEV Profile ==========
# Development: debug-friendly settings

server.port=8080

# Log level: application DEBUG, Spring DEBUG
logging.level.com.inspire12.backend=DEBUG
logging.level.org.springframework.web=DEBUG

# Actuator: expose all endpoints in dev
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# ========== JPA + SQLite ==========
# TODO: Set SQLite datasource URL
# spring.datasource.url=jdbc:sqlite:????
# spring.datasource.driver-class-name=????

# TODO: Add Hibernate settings
# spring.jpa.database-platform=org.hibernate.community.dialect.????
# spring.jpa.hibernate.ddl-auto=????
# spring.jpa.show-sql=????

# Initial data (run data.sql on startup)
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
