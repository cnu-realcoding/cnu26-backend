<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="LOG_DIR" value="./logs"/>
    <property name="LOG_FILE" value="application"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_DIR}/${LOG_FILE}.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${LOG_FILE}-rolling.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/archived/${LOG_FILE}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- ========== 프로필별 로그 설정 ========== -->

    <!-- TODO: <springProfile name="dev"> 블록을 작성하세요 -->
    <!-- DEV: 콘솔만 사용, 애플리케이션 DEBUG 레벨 -->
    <!-- 힌트: <logger name="..." level="..."/>, <root level="..."><appender-ref ref="CONSOLE"/></root> -->

    <!-- TODO: <springProfile name="prod"> 블록을 작성하세요 -->
    <!-- PROD: 콘솔 + 롤링 파일, 애플리케이션 INFO, Spring WARN 레벨 -->

    <!-- TODO: <springProfile name="default"> 블록을 작성하세요 -->
    <!-- 프로필 미지정 시: 콘솔 + 파일, 애플리케이션 DEBUG 레벨 -->

</configuration>
